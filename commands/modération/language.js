const { EmbedBuilder } = require("discord.js");

module.exports = {
  name: "language",
  description: "丨Séléctionne le language du bot.",
  dm: false,
  permission: 8,

  async execute(interaction) {
    
    
  },
};